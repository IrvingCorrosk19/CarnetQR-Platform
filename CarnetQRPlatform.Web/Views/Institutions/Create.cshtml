@model CarnetQRPlatform.Web.Models.CreateInstitutionViewModel
@{
    ViewData["Title"] = "Nueva Empresa";
}

<div class="card fade-in">
    <div class="card-header">
        <h3 class="card-title">
            <i class="fas fa-building me-2"></i>
            Nueva Empresa
        </h3>
    </div>
    <form asp-action="Create" method="post" id="createInstitutionForm" enctype="multipart/form-data">
        <div class="card-body">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <h5 class="mb-3"><i class="fas fa-building me-2"></i>Datos de la Institución</h5>
            <hr />

            <div class="row">
                <div class="col-md-6">
                    <div class="form-group mb-4">
                        <label asp-for="Name" class="form-label">Nombre de la Empresa <span class="text-danger">*</span></label>
                        <input asp-for="Name" class="form-control" placeholder="Ingrese el nombre de la empresa" required />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group mb-4">
                        <label asp-for="CardPrefix" class="form-label">Prefijo de Carnet <span class="text-danger">*</span></label>
                        <input asp-for="CardPrefix" class="form-control" maxlength="10" placeholder="Ej: HEMO" required />
                        <span asp-validation-for="CardPrefix" class="text-danger"></span>
                        <small class="form-text text-muted">Máximo 10 caracteres. Se usará para numerar los carnets (ej: HEMO-0001).</small>
                    </div>
                </div>
            </div>

            <div class="form-group mb-4">
                <label asp-for="Description" class="form-label">Descripción</label>
                <textarea asp-for="Description" class="form-control" rows="3" placeholder="Descripción de la empresa"></textarea>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>

            <div class="row">
                <div class="col-md-4">
                    <div class="form-group mb-4">
                        <label asp-for="Email" class="form-label">Correo Electrónico</label>
                        <input asp-for="Email" type="email" class="form-control" placeholder="correo@empresa.com" />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group mb-4">
                        <label asp-for="Phone" class="form-label">Teléfono</label>
                        <input asp-for="Phone" class="form-control" placeholder="+507 1234-5678" />
                        <span asp-validation-for="Phone" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group mb-4">
                        <label asp-for="InstitutionTypeId" class="form-label">
                            Tipo de Institución <span class="text-danger">*</span>
                            <button type="button" class="btn btn-sm btn-outline-primary ms-2" data-bs-toggle="modal" data-bs-target="#institutionTypeModal" title="Gestionar Tipos">
                                <i class="fas fa-cog"></i>
                            </button>
                        </label>
                        <select asp-for="InstitutionTypeId" class="form-control" id="InstitutionTypeId" required>
                            <option value="">-- Seleccione un tipo --</option>
                            @if (ViewBag.InstitutionTypes != null)
                            {
                                @foreach (var type in (List<CarnetQRPlatform.Domain.Entities.InstitutionType>)ViewBag.InstitutionTypes)
                                {
                                    <option value="@type.Id">@type.Name</option>
                                }
                            }
                        </select>
                        <span asp-validation-for="InstitutionTypeId" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <div class="form-group mb-4">
                <label asp-for="Address" class="form-label">Dirección</label>
                <input asp-for="Address" class="form-control" placeholder="Dirección completa" />
                <span asp-validation-for="Address" class="text-danger"></span>
            </div>

            <hr class="my-4" />
            <h5 class="mb-3"><i class="fas fa-user-shield me-2"></i>Datos del Administrador de la Institución</h5>
            <div class="alert alert-info">
                <i class="fas fa-info-circle me-2"></i>
                Se creará automáticamente un usuario Administrador para esta institución con las credenciales que proporcione.
            </div>
            <hr />

            <div class="row">
                <div class="col-md-6">
                    <div class="form-group mb-4">
                        <label asp-for="AdminEmail" class="form-label">Email del Administrador <span class="text-danger">*</span></label>
                        <input asp-for="AdminEmail" type="email" class="form-control" placeholder="admin@institucion.com" required />
                        <span asp-validation-for="AdminEmail" class="text-danger"></span>
                        <small class="form-text text-muted">Este será el email de acceso del administrador.</small>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group mb-4">
                        <label asp-for="AdminPassword" class="form-label">Contraseña del Administrador <span class="text-danger">*</span></label>
                        <input asp-for="AdminPassword" type="password" class="form-control" placeholder="Mínimo 8 caracteres" required />
                        <span asp-validation-for="AdminPassword" class="text-danger"></span>
                        <small class="form-text text-muted">Debe tener al menos 8 caracteres, mayúsculas, minúsculas, números y símbolos.</small>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6">
                    <div class="form-group mb-4">
                        <label asp-for="AdminFirstName" class="form-label">Nombre del Administrador <span class="text-danger">*</span></label>
                        <input asp-for="AdminFirstName" class="form-control" placeholder="Ingrese el nombre" required />
                        <span asp-validation-for="AdminFirstName" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group mb-4">
                        <label asp-for="AdminLastName" class="form-label">Apellido del Administrador <span class="text-danger">*</span></label>
                        <input asp-for="AdminLastName" class="form-control" placeholder="Ingrese el apellido" required />
                        <span asp-validation-for="AdminLastName" class="text-danger"></span>
                    </div>
                </div>
            </div>
        </div>
        <div class="card-footer">
            <button type="submit" class="btn btn-primary">
                <i class="fas fa-save"></i> Crear Empresa y Administrador
            </button>
            <a asp-action="Index" class="btn btn-secondary">
                <i class="fas fa-times"></i> Cancelar
            </a>
        </div>
    </form>
</div>

<!-- Modal para Gestionar Tipos de Institución -->
<div class="modal fade" id="institutionTypeModal" tabindex="-1" aria-labelledby="institutionTypeModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="institutionTypeModalLabel">
                    <i class="fas fa-building me-2"></i>Gestionar Tipos de Institución
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="mb-3">
                    <button type="button" class="btn btn-primary" id="btnNewType">
                        <i class="fas fa-plus me-2"></i>Nuevo Tipo
                    </button>
                </div>
                
                <div id="typeListContainer">
                    <table class="table table-striped" id="typesTable">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>Descripción</th>
                                <th>Estado</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody id="typesTableBody">
                            <tr>
                                <td colspan="4" class="text-center">
                                    <i class="fas fa-spinner fa-spin"></i> Cargando...
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>

                <!-- Formulario para Crear/Editar -->
                <div id="typeFormContainer" style="display:none;">
                    <hr />
                    <form id="typeForm">
                        <input type="hidden" id="typeId" />
                        <div class="mb-3">
                            <label for="typeName" class="form-label">Nombre <span class="text-danger">*</span></label>
                            <input type="text" class="form-control" id="typeName" required maxlength="100" />
                        </div>
                        <div class="mb-3">
                            <label for="typeDescription" class="form-label">Descripción</label>
                            <textarea class="form-control" id="typeDescription" rows="3"></textarea>
                        </div>
                        <div class="mb-3">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="typeIsActive" checked>
                                <label class="form-check-label" for="typeIsActive">
                                    Activo
                                </label>
                            </div>
                        </div>
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save me-2"></i>Guardar
                            </button>
                            <button type="button" class="btn btn-secondary" id="btnCancelType">
                                <i class="fas fa-times me-2"></i>Cancelar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        $(document).ready(function() {
            $('#createInstitutionForm').on('submit', function(e) {
                e.preventDefault();
                submitFormAjax(this, {
                    loadingMessage: 'Creando empresa y administrador...',
                    successMessage: 'Empresa y administrador creados exitosamente'
                });
            });

            // Cargar tipos cuando se abre el modal
            $('#institutionTypeModal').on('show.bs.modal', function() {
                loadInstitutionTypes();
            });

            // Botón nuevo tipo
            $('#btnNewType').on('click', function() {
                resetTypeForm();
                $('#typeFormContainer').slideDown();
                $('#btnNewType').hide();
            });

            // Botón cancelar formulario
            $('#btnCancelType').on('click', function() {
                $('#typeFormContainer').slideUp();
                $('#btnNewType').show();
                resetTypeForm();
            });

            // Submit del formulario
            $('#typeForm').on('submit', function(e) {
                e.preventDefault();
                saveInstitutionType();
            });

            function loadInstitutionTypes() {
                $.ajax({
                    url: '/api/InstitutionTypes',
                    type: 'GET',
                    success: function(types) {
                        var tbody = $('#typesTableBody');
                        tbody.empty();
                        
                        if (types.length === 0) {
                            tbody.append('<tr><td colspan="4" class="text-center text-muted">No hay tipos de institución registrados.</td></tr>');
                            return;
                        }

                        types.forEach(function(type) {
                            var statusBadge = type.isActive 
                                ? '<span class="badge bg-success">Activo</span>' 
                                : '<span class="badge bg-secondary">Inactivo</span>';
                            
                            var row = '<tr data-id="' + type.id + '">' +
                                '<td>' + type.name + '</td>' +
                                '<td>' + (type.description || '-') + '</td>' +
                                '<td>' + statusBadge + '</td>' +
                                '<td>' +
                                    '<button class="btn btn-sm btn-primary edit-type-btn" data-id="' + type.id + '" title="Editar">' +
                                        '<i class="fas fa-edit"></i>' +
                                    '</button> ' +
                                    '<button class="btn btn-sm ' + (type.isActive ? 'btn-warning' : 'btn-success') + ' toggle-type-btn" data-id="' + type.id + '" data-active="' + type.isActive + '" title="' + (type.isActive ? 'Desactivar' : 'Activar') + '">' +
                                        '<i class="fas fa-' + (type.isActive ? 'ban' : 'check') + '"></i>' +
                                    '</button> ' +
                                    '<button class="btn btn-sm btn-danger delete-type-btn" data-id="' + type.id + '" data-name="' + type.name + '" title="Eliminar">' +
                                        '<i class="fas fa-trash"></i>' +
                                    '</button>' +
                                '</td>' +
                                '</tr>';
                            tbody.append(row);
                        });
                    },
                    error: function() {
                        Swal.fire({
                            icon: 'error',
                            title: 'Error',
                            text: 'No se pudieron cargar los tipos de institución.'
                        });
                    }
                });
            }

            function resetTypeForm() {
                $('#typeId').val('');
                $('#typeName').val('');
                $('#typeDescription').val('');
                $('#typeIsActive').prop('checked', true);
            }

            function saveInstitutionType() {
                var typeData = {
                    id: $('#typeId').val() || '00000000-0000-0000-0000-000000000000',
                    name: $('#typeName').val(),
                    description: $('#typeDescription').val(),
                    isActive: $('#typeIsActive').is(':checked')
                };

                var url = '/api/InstitutionTypes';
                var method = $('#typeId').val() ? 'PUT' : 'POST';

                $.ajax({
                    url: url + (method === 'PUT' ? '/' + $('#typeId').val() : ''),
                    type: method,
                    contentType: 'application/json',
                    data: JSON.stringify(typeData),
                    success: function(response) {
                        if (response.success) {
                            Swal.fire({
                                icon: 'success',
                                title: '¡Guardado!',
                                text: response.message,
                                timer: 2000,
                                showConfirmButton: false
                            });
                            $('#typeFormContainer').slideUp();
                            $('#btnNewType').show();
                            resetTypeForm();
                            loadInstitutionTypes();
                            loadTypesForSelect();
                        } else {
                            Swal.fire({
                                icon: 'error',
                                title: 'Error',
                                text: response.message
                            });
                        }
                    },
                    error: function(xhr) {
                        var errorMsg = 'Error al guardar el tipo de institución.';
                        if (xhr.responseJSON && xhr.responseJSON.message) {
                            errorMsg = xhr.responseJSON.message;
                        }
                        Swal.fire({
                            icon: 'error',
                            title: 'Error',
                            text: errorMsg
                        });
                    }
                });
            }

            function loadTypesForSelect() {
                $.ajax({
                    url: '/api/InstitutionTypes',
                    type: 'GET',
                    success: function(types) {
                        var select = $('#InstitutionTypeId');
                        var currentValue = select.val();
                        select.empty();
                        select.append('<option value="">-- Seleccione un tipo --</option>');
                        
                        types.filter(t => t.isActive).forEach(function(type) {
                            var option = $('<option></option>')
                                .attr('value', type.id)
                                .text(type.name);
                            if (type.id === currentValue) {
                                option.attr('selected', 'selected');
                            }
                            select.append(option);
                        });
                    }
                });
            }

            // Editar tipo
            $(document).on('click', '.edit-type-btn', function() {
                var typeId = $(this).data('id');
                $.ajax({
                    url: '/api/InstitutionTypes/' + typeId,
                    type: 'GET',
                    success: function(type) {
                        $('#typeId').val(type.id);
                        $('#typeName').val(type.name);
                        $('#typeDescription').val(type.description || '');
                        $('#typeIsActive').prop('checked', type.isActive);
                        $('#typeFormContainer').slideDown();
                        $('#btnNewType').hide();
                    },
                    error: function() {
                        Swal.fire({
                            icon: 'error',
                            title: 'Error',
                            text: 'No se pudo cargar el tipo de institución.'
                        });
                    }
                });
            });

            // Toggle activo/inactivo
            $(document).on('click', '.toggle-type-btn', function() {
                var typeId = $(this).data('id');
                var isActive = $(this).data('active');
                
                $.ajax({
                    url: '/api/InstitutionTypes/' + typeId + '/toggle-active',
                    type: 'POST',
                    success: function(response) {
                        if (response.success) {
                            Swal.fire({
                                icon: 'success',
                                title: '¡Actualizado!',
                                text: response.message,
                                timer: 2000,
                                showConfirmButton: false
                            });
                            loadInstitutionTypes();
                            loadTypesForSelect();
                        }
                    },
                    error: function(xhr) {
                        var errorMsg = 'Error al actualizar el estado.';
                        if (xhr.responseJSON && xhr.responseJSON.message) {
                            errorMsg = xhr.responseJSON.message;
                        }
                        Swal.fire({
                            icon: 'error',
                            title: 'Error',
                            text: errorMsg
                        });
                    }
                });
            });

            // Eliminar tipo
            $(document).on('click', '.delete-type-btn', function() {
                var typeId = $(this).data('id');
                var typeName = $(this).data('name');
                
                Swal.fire({
                    title: '¿Estás seguro?',
                    html: `¿Deseas eliminar el tipo de institución <strong>${typeName}</strong>?<br><br>Esta acción no se puede deshacer.`,
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#d33',
                    cancelButtonColor: '#3085d6',
                    confirmButtonText: 'Sí, eliminar',
                    cancelButtonText: 'Cancelar'
                }).then((result) => {
                    if (result.isConfirmed) {
                        $.ajax({
                            url: '/api/InstitutionTypes/' + typeId,
                            type: 'DELETE',
                            success: function(response) {
                                if (response.success) {
                                    Swal.fire({
                                        icon: 'success',
                                        title: '¡Eliminado!',
                                        text: response.message,
                                        timer: 2000,
                                        showConfirmButton: false
                                    });
                                    loadInstitutionTypes();
                                    loadTypesForSelect();
                                } else {
                                    Swal.fire({
                                        icon: 'error',
                                        title: 'Error',
                                        text: response.message
                                    });
                                }
                            },
                            error: function(xhr) {
                                var errorMsg = 'Error al eliminar el tipo de institución.';
                                if (xhr.responseJSON && xhr.responseJSON.message) {
                                    errorMsg = xhr.responseJSON.message;
                                }
                                Swal.fire({
                                    icon: 'error',
                                    title: 'Error',
                                    text: errorMsg
                                });
                            }
                        });
                    }
                });
            });
        });
    </script>
}
